spring:
  application:
    name: productsApi
  datasource:
    url: jdbc:h2:mem:products
    username: sa
    password: pass
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console # path to access database on browser